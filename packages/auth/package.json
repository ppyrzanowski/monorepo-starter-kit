{
  "name": "auth",
  "version": "0.0.0",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json"
  },
  "dependencies": {
    "express": "catalog:",
    "better-auth": "^1.2.12",
    "config": "workspace:*",
    "db": "workspace:*",
    "validator": "workspace:*"
  },
  "devDependencies": {
    "@types/express": "catalog:",
    "typescript": "^5.8.3"
  }
}
